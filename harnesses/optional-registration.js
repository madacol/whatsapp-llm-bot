/**
 * Register optional harness implementations that may not be installed in every
 * environment. Built-in ACP adapters are registered by the main registry.
 * @returns {Promise<void>}
 */
export async function registerOptionalHarnesses() {
}
