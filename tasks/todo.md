# Tasks

## Todo

- Harden webpage deployments so managed pages are private by default; use `/tmp/deploy-webpage-security-handoff.md` as the implementation handoff.
- Make Codex ACP file edits ask for approval reliably and render pending diffs by correlating `tool_call` diff updates with `session/request_permission` by `toolCallId`.

## Doing

## Blocked
